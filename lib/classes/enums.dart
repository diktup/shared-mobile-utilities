// ignore_for_file: constant_identifier_names

enum DeviceTheme {
  SYSTEM,
  DARK,
  LIGHT,
}

enum SlideDirection {
  Down,
  Up,
}

enum LinePosition {
  left,
  top,
  right,
  bottom,
}

enum Shape {
  line,
  box,
  circle,
}
